chicanery
=========

A simple wrapper for the backup tool `duplicity`.

Currently, it has these commands:

 * genkey - generate new GPG keys to be used for backups
 * backup - run a backup using the configuration found in the config file

